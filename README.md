Right‑click a request/response → Run external EXE (UI).

Enter the absolute EXE path and arguments in the dialog.

The output window should appear centered and on top (briefly), then show process output.